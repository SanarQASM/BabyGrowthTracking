<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — UPDATED FOR LANDSCAPE SUPPORT

    Key changes:
    ✅  android:screenOrientation is NOT set (or was never set) — the activity
        supports all orientations by default.
    ✅  android:configChanges includes orientation|screenSize|screenLayout|keyboardHidden
        so Android does NOT restart the Activity on rotation — Compose recomposes
        in-place and LocalConfiguration updates, driving the responsive layout.

    NO OTHER CHANGES — all permissions, meta-data, and providers are identical
    to the original file.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.BabyGrowth"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />

        <activity
            android:name="org.example.project.babygrowthtrackingapplication.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:theme="@style/Theme.BabyGrowth"
            android:windowSoftInputMode="adjustResize">
            <!--
                ✅  NO android:screenOrientation attribute here.
                    Omitting it means "fullSensor" — all orientations supported.
                    If you previously had screenOrientation="portrait", REMOVE IT.

                ✅  configChanges prevents Activity recreation on rotation.
                    Compose reads LocalConfiguration.current which updates
                    automatically — no restart, no layout jump.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

    <queries>
        <package android:name="com.google.android.gms" />
        <package android:name="com.android.chrome" />
        <intent>
            <action android:name="android.service.credentials.CredentialProviderService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

</manifest>